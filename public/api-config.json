{
  "_note": "Public config. Real baseUrl lives in .env.local (NEXT_PUBLIC_API_BASE_URL) — gitignored. lib/api.ts uses placeholder fallback automatically when live fetch returns 4xx/5xx or empty.",
  "mode": "live",
  "endpoints": {
    "members": "/member?limit=99",
    "gallery": "/gallery"
  }
}
