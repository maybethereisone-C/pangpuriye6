---
phase: "02-stitch-design-pass"
plan: "02-02"
type: "docs"
wave: 1
depends_on: []
files_modified:
  - "assets/raw/stitch/04-gallery/04-gallery-desktop.png"
  - "assets/raw/stitch/04-gallery/04-gallery-tablet.png"
  - "assets/raw/stitch/04-gallery/04-gallery-mobile.png"
  - "assets/raw/stitch/04-gallery/04-gallery-stitch-url.txt"
  - "assets/raw/stitch/05-recognition/05-recognition-desktop.png"
  - "assets/raw/stitch/05-recognition/05-recognition-tablet.png"
  - "assets/raw/stitch/05-recognition/05-recognition-mobile.png"
  - "assets/raw/stitch/05-recognition/05-recognition-stitch-url.txt"
  - "assets/raw/stitch/06-clips/06-clips-desktop.png"
  - "assets/raw/stitch/06-clips/06-clips-tablet.png"
  - "assets/raw/stitch/06-clips/06-clips-mobile.png"
  - "assets/raw/stitch/06-clips/06-clips-stitch-url.txt"
  - "docs/stitch-prompts.md"
autonomous: false
requirements:
  - "DESIGN-05"
  - "DESIGN-06"
  - "DESIGN-07"
must_haves:
  truths:
    - "Every locked frame respects the design tokens in app/globals.css (palette, type, grid, breakpoints)"
    - "All copy in frames is English only — no Thai characters"
    - "No production code (app/, components/, lib/, styles/) is modified this phase"
    - "Sections 5–7 inherit the design system that Home locked in 02-01 — visually consistent with 02-01 outputs"
    - "Each section reviewed in-chat with image attachment; Tew typed 'lock' or 'approved'"
    - "Max 2 iterations per section — third try means the prompt is broken (escalate, rewrite prompt)"
  artifacts:
    - "assets/raw/stitch/04-gallery/04-gallery-desktop.png"
    - "assets/raw/stitch/04-gallery/04-gallery-tablet.png"
    - "assets/raw/stitch/04-gallery/04-gallery-mobile.png"
    - "assets/raw/stitch/05-recognition/05-recognition-desktop.png"
    - "assets/raw/stitch/05-recognition/05-recognition-tablet.png"
    - "assets/raw/stitch/05-recognition/05-recognition-mobile.png"
    - "assets/raw/stitch/06-clips/06-clips-desktop.png"
    - "assets/raw/stitch/06-clips/06-clips-tablet.png"
    - "assets/raw/stitch/06-clips/06-clips-mobile.png"
    - "docs/stitch-prompts.md updated with the final-locked prompt for sections 5–7"
  key_links:
    - "Locked frame palette ↔ app/globals.css @theme tokens (hex must match)"
    - "Locked frame typography ↔ Space Grotesk display, IBM Plex Mono captions, PP Supply Mono eyebrows"
    - "Section 5–7 visual style ↔ Section 1 (Home) lock — same design system inherited"
    - "docs/stitch-prompts.md Prompt #N text ↔ the actual prompt run in Stitch that produced the saved PNG"
---

<objective>
Run Google Stitch for Sections 5–7 (Gallery / Recognition / Clips) of pangpuriye6. Generate desktop / tablet / mobile frames per section, iterate with Tew (max 2 cycles each), save locked PNGs to `assets/raw/stitch/<section>/`, and update `docs/stitch-prompts.md` Prompts #5–#7 with the final-locked text.

Purpose: Lock the visual bar for Phase 05 (Gallery + Recognition + Clips coding). These three sections inherit the design system already locked by Home in plan 02-01, so the design system itself is settled — only per-section composition decisions remain.
Output: 9 locked PNG frames (3 sections × 3 viewports), 3 `stitch-url.txt` files, and 3 updated prompt blocks in `docs/stitch-prompts.md`.

NOTE on parallelism: This plan is wave 1 (no plan dependencies). Sections 5–7 inherit the design system from `app/globals.css` directly — the system does NOT change as a result of plan 02-01. If Tew wants strict serial order (Home lock before touching Gallery), that is a workflow choice, not a plan dependency, and is honored in the execute step (single Stitch project, sections opened sequentially per D-02).
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-stitch-design-pass/02-SPEC.md
@.planning/phases/02-stitch-design-pass/02-CONTEXT.md
@docs/design.md
@docs/stitch-prompts.md
@app/globals.css
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-02-T01: Lock Gallery (Section 04) Stitch frames — DESIGN-05</name>
  <files>assets/raw/stitch/04-gallery/04-gallery-desktop.png, assets/raw/stitch/04-gallery/04-gallery-tablet.png, assets/raw/stitch/04-gallery/04-gallery-mobile.png, assets/raw/stitch/04-gallery/04-gallery-stitch-url.txt, docs/stitch-prompts.md</files>
  <action>
    Run Stitch for Gallery (Section 04) — editorial photo grid + horizontal-scroll rail.

    Steps:
    1. Open the same Stitch project from plan 02-01 (single project for all 7 sections, per D-01). Save URL into `assets/raw/stitch/04-gallery/04-gallery-stitch-url.txt`.
    2. Paste shared header + Prompt #5 from `docs/stitch-prompts.md`. Attach both reference screenshots `assets/raw/references/obys-home.png` + `assets/raw/references/namma-home.png` (per D-05).
    3. Generate frames at desktop 1440×900, tablet 834×1194, mobile 390×844.
    4. Paste frames into chat for Tew review. Max 2 iterations (per D-04).
    5. Discard failed iterations (per D-11).
    6. Save locked PNGs to `assets/raw/stitch/04-gallery/04-gallery-{desktop|tablet|mobile}.png` (per D-08). Save HTML/CSS exports alongside if Stitch offers them (per D-07).
    7. Update `docs/stitch-prompts.md` Prompt #5 with the final-locked text (per D-13).
    8. Record iteration count + rejection reasons for commit body (per D-12).

    Design-token verification (per docs/design.md §3.5 row 04 + Prompt #5):
    - Header: eyebrow `SEC.04 · GALLERY // /records/pangpuriye/2026` in PP Supply Mono. H2 `House Activities` in Space Grotesk 700.
    - Right-aligned mono pills: `QUERY: SUCCESS` + `42 RECORDS` (IBM Plex Mono, 0-corner pills).
    - Event divider: full-width hairline `#C9C5BE` with mono left tag `SEC.ID: HACK-01` + center title in Space Grotesk 700 + right date.
    - Featured grid: 8-col big photo (16:9, b&w grayscale) with overlay top-left carrying mono badge `OBJ.ID: 0001 · LOC: TBD`. Caption below in IBM Plex Mono.
    - Right 4-col stack: smaller photo + house metrics card (two progress bars labeled `Models Shipped` + `Hackathons Joined`, RED `#C1121F` fill — confirms surgical-red usage on bars only, not their tracks).
    - Second event divider, then asymmetric two-photo row (4-col portrait + 8-col landscape).
    - Tablet: photos stack to 6+6 and 12 wide.
    - Mobile: all photos full-width, single column, captions below each.
    - English copy only (event titles can be `Lorem Hackathon Title`-style placeholders per Prompt #5).
  </action>
  <what-built>Three locked Stitch frame PNGs for Section 04 (Gallery) + final prompt text in `docs/stitch-prompts.md` Prompt #5.</what-built>
  <how-to-verify>
    1. Open all three PNGs at `assets/raw/stitch/04-gallery/04-gallery-{desktop,tablet,mobile}.png`. Confirm: featured 8-col 16:9 photo with `OBJ.ID` overlay badge; 4-col right stack with two red-fill progress bars; two event dividers; asymmetric 4+8 photo row.
    2. Confirm photo treatments are b&w / high-contrast grayscale per shared-header HARD-NO ("no Instagram filters").
    3. Confirm `04-gallery-stitch-url.txt` exists.
    4. Confirm Prompt #5 updated in `docs/stitch-prompts.md`.
    5. Confirm progress bars use `#C1121F` fill on charcoal/cream tracks only — no other red bleed into Gallery.
    6. Confirm English copy only.
  </how-to-verify>
  <resume-signal>Tew types "lock" or "approved" (or revision ask, max 2 iterations).</resume-signal>
  <verify>
    <automated>test -f assets/raw/stitch/04-gallery/04-gallery-desktop.png && test -f assets/raw/stitch/04-gallery/04-gallery-tablet.png && test -f assets/raw/stitch/04-gallery/04-gallery-mobile.png && test -f assets/raw/stitch/04-gallery/04-gallery-stitch-url.txt</automated>
  </verify>
  <done>
    - [ ] Three PNG files exist at `assets/raw/stitch/04-gallery/04-gallery-{desktop,tablet,mobile}.png`
    - [ ] `04-gallery-stitch-url.txt` contains Stitch project URL
    - [ ] Featured 8-col 16:9 photo with `OBJ.ID` overlay badge present
    - [ ] House metrics card has two progress bars with `#C1121F` red fill
    - [ ] Two event dividers (`SEC.ID: HACK-01` style) with full-width hairlines
    - [ ] Asymmetric 4+8 photo row in the desktop frame
    - [ ] Photo treatment is b&w / grayscale (no color filters)
    - [ ] Typography: Space Grotesk + IBM Plex Mono + PP Supply Mono
    - [ ] English copy only
    - [ ] `docs/stitch-prompts.md` Prompt #5 updated to final-locked version
    - [ ] Tew typed "lock" / "approved" — `Reviewed-by: Tew` in commit
    - [ ] Iteration count + rejection reasons in commit body
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-02-T02: Lock Recognition (Section 05) Stitch frames — DESIGN-06</name>
  <files>assets/raw/stitch/05-recognition/05-recognition-desktop.png, assets/raw/stitch/05-recognition/05-recognition-tablet.png, assets/raw/stitch/05-recognition/05-recognition-mobile.png, assets/raw/stitch/05-recognition/05-recognition-stitch-url.txt, docs/stitch-prompts.md</files>
  <action>
    Run Stitch for Recognition (Section 05) — award trophy display + numbered milestones.

    Steps:
    1. Open the same Stitch project. Save URL into `assets/raw/stitch/05-recognition/05-recognition-stitch-url.txt`.
    2. Paste shared header + Prompt #6 from `docs/stitch-prompts.md`. Attach both reference screenshots (per D-05).
    3. Generate frames at desktop 1440×900, tablet 834×1194, mobile 390×844.
    4. Paste frames into chat for Tew review. Max 2 iterations (per D-04).
    5. Discard failed iterations (per D-11).
    6. Save locked PNGs to `assets/raw/stitch/05-recognition/05-recognition-{desktop|tablet|mobile}.png`.
    7. Update `docs/stitch-prompts.md` Prompt #6 with the final-locked text.
    8. Record iteration count + rejection reasons for commit body.

    Design-token verification (per docs/design.md §3.5 row 05 + Prompt #6):
    - Header: eyebrow `DIRECTORY / HALL_OF_FAME` in PP Supply Mono. H2 `Recognition Archive` in Space Grotesk 700. Lead paragraph below in body style.
    - 8-col hero award card with charcoal `#1A1A1A` hairline border. Mono code `AWD-XX-01` in red `#C1121F`. Outline lightbulb icon (single line, no fill). Title `Innovation Award` in Space Grotesk 700 28px. Description body. Two pill tags (0-corner, charcoal hairline).
    - 4-col stack: 2 smaller award cards. Each has red class badge `CLASS-X`, medal outline icon, title, `Recipient: <name>` line in IBM Plex Mono.
    - Below: 6+6 row. Left = Best Team Project card with progress bar (red fill). Right = Milestones numbered list with BIG RED `01` / `02` numerals on left, title + description right.
    - Tablet: hero stays 8-col, secondaries collapse below.
    - Mobile: all cards stack single column.
    - English copy only.
  </action>
  <what-built>Three locked Stitch frame PNGs for Section 05 (Recognition) + final prompt text in `docs/stitch-prompts.md` Prompt #6.</what-built>
  <how-to-verify>
    1. Open all three PNGs. Confirm: 8-col hero award card with `AWD-XX-01` red mono code + lightbulb outline icon + `Innovation Award` title; 4-col stack with 2 secondary award cards; 6+6 row with progress bar + numbered milestone list (big red numerals).
    2. Confirm all icons are OUTLINE style (no fills, no drop shadows per shared header HARD NOs).
    3. Confirm `05-recognition-stitch-url.txt` exists.
    4. Confirm Prompt #6 updated in `docs/stitch-prompts.md`.
    5. Confirm zero rounded corners on pills / badges / cards (max 2px corners).
    6. Confirm English copy only.
  </how-to-verify>
  <resume-signal>Tew types "lock" or "approved" (or revision ask, max 2 iterations).</resume-signal>
  <verify>
    <automated>test -f assets/raw/stitch/05-recognition/05-recognition-desktop.png && test -f assets/raw/stitch/05-recognition/05-recognition-tablet.png && test -f assets/raw/stitch/05-recognition/05-recognition-mobile.png && test -f assets/raw/stitch/05-recognition/05-recognition-stitch-url.txt</automated>
  </verify>
  <done>
    - [ ] Three PNG files exist at `assets/raw/stitch/05-recognition/05-recognition-{desktop,tablet,mobile}.png`
    - [ ] `05-recognition-stitch-url.txt` contains Stitch project URL
    - [ ] 8-col hero award card with `AWD-XX-01` red mono code + outline lightbulb icon
    - [ ] `Innovation Award` title in Space Grotesk 700 28px
    - [ ] 4-col stack with 2 secondary cards, each with `CLASS-X` red badge + outline medal icon
    - [ ] 6+6 row: progress bar (red fill) + numbered milestones (big red `01`/`02`)
    - [ ] All icons outline (no fills, no shadows)
    - [ ] Zero rounded corners on chips / cards / badges
    - [ ] English copy only
    - [ ] `docs/stitch-prompts.md` Prompt #6 updated
    - [ ] Tew typed "lock" / "approved" — `Reviewed-by: Tew` in commit
    - [ ] Iteration count + rejection reasons in commit body
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-02-T03: Lock Clips (Section 06) Stitch frames — DESIGN-07</name>
  <files>assets/raw/stitch/06-clips/06-clips-desktop.png, assets/raw/stitch/06-clips/06-clips-tablet.png, assets/raw/stitch/06-clips/06-clips-mobile.png, assets/raw/stitch/06-clips/06-clips-stitch-url.txt, docs/stitch-prompts.md</files>
  <action>
    Run Stitch for Clips (Section 06) — featured YouTube block + horizontal-scroll rail of secondary clips.

    Steps:
    1. Open the same Stitch project. Save URL into `assets/raw/stitch/06-clips/06-clips-stitch-url.txt`.
    2. Paste shared header + Prompt #7 from `docs/stitch-prompts.md`. Attach both reference screenshots (per D-05).
    3. Generate frames at desktop 1440×900, tablet 834×1194, mobile 390×844.
    4. Paste frames into chat for Tew review. Max 2 iterations (per D-04).
    5. Discard failed iterations (per D-11).
    6. Save locked PNGs to `assets/raw/stitch/06-clips/06-clips-{desktop|tablet|mobile}.png`.
    7. Update `docs/stitch-prompts.md` Prompt #7 with the final-locked text.
    8. Record iteration count + rejection reasons for commit body.

    Design-token verification (per docs/design.md §3.5 row 06 + Prompt #7):
    - Header: eyebrow `SEC.06 · CLIPS // KB.LOG · VIDEO_ARCHIVE` in PP Supply Mono. H2 `Knowledge Clips` in Space Grotesk 700. Right-aligned mono `6 clips`.
    - Topic chips row (mono uppercase, 0-corner): `All / LLM / Computer Vision / RL / MLOps / Ethics / Data`. Hover state on one chip = red `#C1121F` border.
    - Featured clip block: 8-col 16:9 video frame thumbnail. RED `#C1121F` overlay badge `FEATURED` top-right. Duration `12:34` bottom-right in IBM Plex Mono. Center play button: charcoal `#1A1A1A` disk with white/cream triangle.
    - Right 4-col: topic tag pill, date in mono, title in Space Grotesk 700, speaker line, lead paragraph in body style.
    - Horizontal scroll rail: 3–4 secondary clip cards visible at 320px wide each. Each card has thumb + duration + topic chip + title + speaker + date.
    - Tablet: featured collapses to 12-col; rail shows 2.5 cards (cropping affordance).
    - Mobile: featured stacks (thumb above body); rail shows 1.5 cards (swipe affordance).
    - English copy only. Placeholder clip titles like `Lorem clip title` acceptable per shared-header conventions.
  </action>
  <what-built>Three locked Stitch frame PNGs for Section 06 (Clips) + final prompt text in `docs/stitch-prompts.md` Prompt #7.</what-built>
  <how-to-verify>
    1. Open all three PNGs. Confirm: featured 8-col 16:9 video thumb with red `FEATURED` badge top-right + `12:34` duration bottom-right + charcoal play button center; right 4-col body block; horizontal scroll rail with secondary clip cards (3–4 visible desktop, 2.5 tablet, 1.5 mobile).
    2. Confirm play button is solid charcoal disk with white/cream triangle (no gradients, no drop shadows).
    3. Confirm topic chips have zero rounded corners; hover state shows red border on one chip.
    4. Confirm `06-clips-stitch-url.txt` exists.
    5. Confirm Prompt #7 updated in `docs/stitch-prompts.md`.
    6. Confirm English copy only.
  </how-to-verify>
  <resume-signal>Tew types "lock" or "approved" (or revision ask, max 2 iterations).</resume-signal>
  <verify>
    <automated>test -f assets/raw/stitch/06-clips/06-clips-desktop.png && test -f assets/raw/stitch/06-clips/06-clips-tablet.png && test -f assets/raw/stitch/06-clips/06-clips-mobile.png && test -f assets/raw/stitch/06-clips/06-clips-stitch-url.txt</automated>
  </verify>
  <done>
    - [ ] Three PNG files exist at `assets/raw/stitch/06-clips/06-clips-{desktop,tablet,mobile}.png`
    - [ ] `06-clips-stitch-url.txt` contains Stitch project URL
    - [ ] Featured 8-col 16:9 video thumbnail with red `FEATURED` badge + duration overlay + charcoal play button
    - [ ] 7 topic chips visible (`All / LLM / Computer Vision / RL / MLOps / Ethics / Data`), zero rounded corners
    - [ ] One chip in hover state with red `#C1121F` border
    - [ ] Horizontal scroll rail visible — desktop 3–4 cards, tablet 2.5 cards, mobile 1.5 cards
    - [ ] Cards are 320px wide on desktop frame
    - [ ] Typography: Space Grotesk + IBM Plex Mono + PP Supply Mono
    - [ ] English copy only
    - [ ] `docs/stitch-prompts.md` Prompt #7 updated
    - [ ] Tew typed "lock" / "approved" — `Reviewed-by: Tew` in commit
    - [ ] Iteration count + rejection reasons in commit body
  </done>
</task>

</tasks>

<verification>
  After all three tasks complete:
  1. `ls assets/raw/stitch/04-gallery/ assets/raw/stitch/05-recognition/ assets/raw/stitch/06-clips/` shows desktop / tablet / mobile PNGs + stitch-url.txt in each.
  2. `git diff -- app/ components/ lib/ styles/` returns empty (no production code modified).
  3. `docs/stitch-prompts.md` Prompts #5–#7 reflect the exact final-locked text per section.
  4. Commit message body includes iteration counts + rejection reasons per section + `Reviewed-by: Tew`.
  5. No Thai characters anywhere in PNGs or HTML/CSS.
  6. All three sections' frames feel visually consistent with the Home / About / Members / Red Wall frames locked in plan 02-01 — same palette, type, grid usage.
</verification>

<success_criteria>
  - DESIGN-05 (Gallery) locked: 3 frames + final prompt + Tew approval.
  - DESIGN-06 (Recognition) locked: 3 frames + final prompt + Tew approval.
  - DESIGN-07 (Clips) locked: 3 frames + final prompt + Tew approval.
  - All locked frames pass design-token fidelity check.
  - All copy is English only.
  - No production code modified this plan.
  - `docs/stitch-prompts.md` Prompts #5–#7 match the prompts that produced the saved PNGs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stitch-design-pass/02-02-SUMMARY.md` per the execute-plan template, capturing:
- Frames locked per section (with iteration count)
- Tew's rejection notes per iteration
- Prompt rewrites required (track drift from original draft)
- Forward signal to Phase 05 coders: photo treatments, embed sizing, rail-card pixel-targets, progress-bar fill rules, milestone numeral sizing
- Cross-plan consistency note: does the visual feel match plan 02-01's Home / About / Members / Red Wall?
</output>
