---
phase: "02-stitch-design-pass"
plan: "02-03"
type: "docs"
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - "assets/raw/stitch/07-others/07-others-desktop.png"
  - "assets/raw/stitch/07-others/07-others-tablet.png"
  - "assets/raw/stitch/07-others/07-others-mobile.png"
  - "assets/raw/stitch/07-others/07-others-stitch-url.txt"
  - "docs/stitch-prompts.md"
autonomous: false
optional: true
requirements: []
must_haves:
  truths:
    - "OPTIONAL plan — only run if 02-01 + 02-02 lock with time remaining before Phase 03 starts (per D-14)"
    - "If skipped, no requirement coverage is lost: Section 7 Others is not in DESIGN-01..07 and is deferred-friendly"
    - "If run: locked frame respects the design tokens in app/globals.css"
    - "If run: all copy in frames is English only"
    - "If run: no production code (app/, components/, lib/, styles/) is modified"
  artifacts:
    - "assets/raw/stitch/07-others/07-others-desktop.png (only if run)"
    - "assets/raw/stitch/07-others/07-others-tablet.png (only if run)"
    - "assets/raw/stitch/07-others/07-others-mobile.png (only if run)"
    - "docs/stitch-prompts.md Prompt #8 updated (only if run)"
  key_links:
    - "If run: locked frame palette ↔ app/globals.css @theme tokens"
    - "If run: Others.tsx scaffold (`data.others.enabled = false` default, per CONTEXT.md D-14) — Stitch frame feeds Phase 06 coding if enabled"
---

<objective>
**OPTIONAL PLAN — do not run unless 02-01 + 02-02 are both locked AND meaningful slack remains before Phase 03 (Home coding) is due to start.**

If conditions met: run Google Stitch for Section 7 (Others — Memes & Brainstorms creative wall), generate desktop / tablet / mobile frames, iterate with Tew (max 2 cycles), save locked PNGs to `assets/raw/stitch/07-others/`, and update `docs/stitch-prompts.md` Prompt #8 with the final-locked text.

If conditions NOT met: skip this plan entirely. Phase 02 verification still passes — Section 7 Others is NOT in DESIGN-01..07 (which are the only seven phase-locked requirements). `Others.tsx` scaffold defaults to `data.others.enabled = false`, so the section stays hidden until enabled.

Purpose: Optional buffer for a creative-wall section if time allows. Decisioned out-of-scope per CONTEXT.md D-14 ("Defer Section 7 Others until 7 required sections are locked").
Output (if run): 3 locked PNG frames + 1 stitch-url.txt + updated Prompt #8.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-stitch-design-pass/02-SPEC.md
@.planning/phases/02-stitch-design-pass/02-CONTEXT.md
@docs/design.md
@docs/stitch-prompts.md
@app/globals.css
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 02-03-T01: Decide whether to run Section 7 Others</name>
  <decision>Run Section 7 (Others) Stitch lock now, or skip and defer to Phase 06 (Others coding) prep?</decision>
  <context>
    Per CONTEXT.md D-14, Section 7 Others is deferred — optional add-on after sections 1–6 lock. ROADMAP Phase 06 already covers Others coding; the Stitch frame for Others can attach there if needed.

    Section 7 Others is NOT in DESIGN-01..07. If skipped here, Phase 02 verification still passes (acceptance criteria in 02-SPEC.md only list the 7 required sections + their dirs — Section 7 / 07-others is mentioned only as "optional, generate last").

    Decision drivers:
    - Time remaining before Phase 03 (Home coding) is due to start.
    - Tew's energy / fatigue after 7 Stitch locks back-to-back.
    - Whether `placeholderSiteData.others.enabled` is likely to flip to `true` in v1 ship window. If staying false, locking the Stitch frame now is sunk effort until Phase 06 fires.
  </context>
  <options>
    <option id="run-now">
      <name>Run Section 7 Others Stitch lock now (within Phase 02)</name>
      <pros>
        - Phase 02 ships visually complete for all 8 sections.
        - Phase 06 (Others coding) has a locked target before Phase 03–05 land.
        - Stitch project + design system is fresh in muscle memory — efficient single sitting.
      </pros>
      <cons>
        - Sunk effort if `others.enabled` stays false through ship.
        - Slows Phase 03 (Home coding) start.
        - Tew may be fatigued after 7 prior locks in 02-01 + 02-02.
      </cons>
    </option>
    <option id="defer">
      <name>Skip and defer to Phase 06 prep (recommended per D-14)</name>
      <pros>
        - Honors D-14 default ("defer — optional after 1–7 lock").
        - Frees time for Phase 03 (Home coding) which is on the critical path.
        - Avoids work that may be discarded if `others.enabled` stays false.
        - If Phase 06 fires, Stitch lock can run as a wave-0 task there with no loss of context.
      </pros>
      <cons>
        - Phase 06 picks up an extra Stitch session.
        - Phase 02 SUMMARY notes 1 optional item dropped (cosmetic only — not a coverage gap).
      </cons>
    </option>
  </options>
  <resume-signal>Tew selects: `run-now` or `defer` (defer is the documented default per D-14).</resume-signal>
  <verify>
    <automated>echo "decision-only task; gates the remaining tasks in this plan"</automated>
  </verify>
  <done>
    - [ ] Tew's choice (`run-now` or `defer`) recorded in chat
    - [ ] If `defer`: rest of this plan is no-op. Mark plan 02-03 as `SKIPPED — deferred per D-14`. Phase 02 verification proceeds without 02-03.
    - [ ] If `run-now`: proceed to Task 02-03-T02.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-03-T02: Lock Others (Section 7) Stitch frames — OPTIONAL, only if T01 = run-now</name>
  <files>assets/raw/stitch/07-others/07-others-desktop.png, assets/raw/stitch/07-others/07-others-tablet.png, assets/raw/stitch/07-others/07-others-mobile.png, assets/raw/stitch/07-others/07-others-stitch-url.txt, docs/stitch-prompts.md</files>
  <action>
    ONLY run this task if Task 02-03-T01 resolved to `run-now`. Otherwise skip — plan 02-03 is marked SKIPPED.

    Steps:
    1. Open the same Stitch project used in plans 02-01 + 02-02 (single project for all sections, per D-01). Save URL into `assets/raw/stitch/07-others/07-others-stitch-url.txt`.
    2. Paste shared header + Prompt #8 from `docs/stitch-prompts.md`. Attach both reference screenshots `assets/raw/references/obys-home.png` + `assets/raw/references/namma-home.png` (per D-05).
    3. Generate frames at desktop 1440×900, tablet 834×1194, mobile 390×844.
    4. Paste frames into chat for Tew review. Max 2 iterations (per D-04).
    5. Discard failed iterations (per D-11).
    6. Save locked PNGs to `assets/raw/stitch/07-others/07-others-{desktop|tablet|mobile}.png` (per D-08).
    7. Update `docs/stitch-prompts.md` Prompt #8 with the final-locked text (per D-13).
    8. Record iteration count + rejection reasons for commit body (per D-12).

    Design-token verification (per docs/design.md §3.5 row 07 + Prompt #8):
    - 4-col bento on desktop with 6+ tiles of varying aspect ratio. Mix of square, portrait, landscape.
    - Each tile background: cream `#FAF7F2`, charcoal `#1A1A1A` hairline border (1px). NO drop shadows (HARD NO per shared header).
    - Mono label top-left: `MEME` or `BRAINSTORM` in red `#C1121F`. Playful caption below in body style.
    - Sticky-note vibe: slight rotation on alternating tiles — `-2deg / +1deg / -1deg`.
    - RED `#C1121F` push-pin dot top-center on a few tiles only — surgical, not on every tile.
    - Tablet: 2-col bento, rotation preserved.
    - Mobile: 1-col bento. Rotation REMOVED for stack legibility (per Prompt #8).
    - English copy only.
  </action>
  <what-built>Three locked Stitch frame PNGs for Section 7 (Others) + final prompt text in `docs/stitch-prompts.md` Prompt #8.</what-built>
  <how-to-verify>
    1. Open all three PNGs at `assets/raw/stitch/07-others/07-others-{desktop,tablet,mobile}.png`. Confirm: 4-col bento desktop with 6+ tiles of varying aspect ratios; mono `MEME` / `BRAINSTORM` red labels; sticky-note rotation `-2deg / +1deg / -1deg` alternating; surgical red push-pin dots on some tiles only.
    2. Confirm tile borders are 1px charcoal hairline only — NO drop shadows.
    3. Confirm mobile frame has 1-col bento WITHOUT rotation (legibility).
    4. Confirm `07-others-stitch-url.txt` exists.
    5. Confirm Prompt #8 updated in `docs/stitch-prompts.md`.
    6. Confirm English copy only.
  </how-to-verify>
  <resume-signal>Tew types "lock" or "approved" (or revision ask, max 2 iterations).</resume-signal>
  <verify>
    <automated>test -f assets/raw/stitch/07-others/07-others-desktop.png && test -f assets/raw/stitch/07-others/07-others-tablet.png && test -f assets/raw/stitch/07-others/07-others-mobile.png && test -f assets/raw/stitch/07-others/07-others-stitch-url.txt</automated>
  </verify>
  <done>
    - [ ] Three PNG files exist at `assets/raw/stitch/07-others/07-others-{desktop,tablet,mobile}.png`
    - [ ] `07-others-stitch-url.txt` contains Stitch project URL
    - [ ] 4-col bento desktop with 6+ tiles of varying aspect ratio
    - [ ] Mono labels `MEME` / `BRAINSTORM` in red `#C1121F` top-left of each tile
    - [ ] Sticky-note rotation `-2deg / +1deg / -1deg` alternating on desktop + tablet
    - [ ] Surgical red push-pin dots on some tiles only (not all)
    - [ ] Tile borders 1px charcoal hairline, no drop shadows
    - [ ] Mobile frame is 1-col, rotation removed
    - [ ] English copy only
    - [ ] `docs/stitch-prompts.md` Prompt #8 updated
    - [ ] Tew typed "lock" / "approved" — `Reviewed-by: Tew` in commit
    - [ ] Iteration count + rejection reasons in commit body
  </done>
</task>

</tasks>

<verification>
  Two cases:

  **Case A — Tew chose `defer` in T01:**
  1. Plan 02-03 SUMMARY notes `SKIPPED — deferred per D-14`.
  2. No files created under `assets/raw/stitch/07-others/`.
  3. `docs/stitch-prompts.md` Prompt #8 remains as draft (not promoted to final-locked).
  4. Phase 02 verification proceeds — Section 7 Others is not part of DESIGN-01..07, so deferral is not a coverage gap.

  **Case B — Tew chose `run-now` in T01:**
  1. `ls assets/raw/stitch/07-others/` shows desktop / tablet / mobile PNGs + stitch-url.txt.
  2. `git diff -- app/ components/ lib/ styles/` returns empty (no production code modified).
  3. `docs/stitch-prompts.md` Prompt #8 reflects the final-locked text.
  4. Commit message body includes iteration count + rejection reasons + `Reviewed-by: Tew`.
  5. No Thai characters in PNGs or HTML/CSS.
</verification>

<success_criteria>
  Plan 02-03 succeeds in EITHER state:
  - SKIPPED — deferred per D-14 (no artifacts created, plan summary records the skip). OR
  - LOCKED — 3 frames + final prompt #8 + Tew approval, all design-token compliant.

  Either resolution is acceptable. Phase 02 verification gate (running `/gsd-verify-work 02`) does NOT require Section 7 frames to exist.
</success_criteria>

<output>
After completion (skip or lock), create `.planning/phases/02-stitch-design-pass/02-03-SUMMARY.md` capturing:
- Tew's T01 decision (run-now or defer).
- If deferred: rationale and downstream impact (Phase 06 picks up the Stitch lock).
- If locked: frames captured, iteration count, rejection notes, forward signal to Phase 06 coders (bento gap sizing, rotation degrees, push-pin dot positioning).
- Cross-plan consistency note (if locked): does the visual feel match plans 02-01 + 02-02?
</output>
