---
phase: "02-stitch-design-pass"
plan: "02-01"
type: "docs"
wave: 1
depends_on: []
files_modified:
  - "assets/raw/stitch/01-home/01-home-desktop.png"
  - "assets/raw/stitch/01-home/01-home-tablet.png"
  - "assets/raw/stitch/01-home/01-home-mobile.png"
  - "assets/raw/stitch/01-home/01-home-stitch-url.txt"
  - "assets/raw/stitch/02-about/02-about-desktop.png"
  - "assets/raw/stitch/02-about/02-about-tablet.png"
  - "assets/raw/stitch/02-about/02-about-mobile.png"
  - "assets/raw/stitch/02-about/02-about-stitch-url.txt"
  - "assets/raw/stitch/03-members/03-members-desktop.png"
  - "assets/raw/stitch/03-members/03-members-tablet.png"
  - "assets/raw/stitch/03-members/03-members-mobile.png"
  - "assets/raw/stitch/03-members/03-members-stitch-url.txt"
  - "assets/raw/stitch/tr-red-wall/tr-red-wall-desktop.png"
  - "assets/raw/stitch/tr-red-wall/tr-red-wall-tablet.png"
  - "assets/raw/stitch/tr-red-wall/tr-red-wall-mobile.png"
  - "assets/raw/stitch/tr-red-wall/tr-red-wall-stitch-url.txt"
  - "docs/stitch-prompts.md"
autonomous: false
requirements:
  - "DESIGN-01"
  - "DESIGN-02"
  - "DESIGN-03"
  - "DESIGN-04"
must_haves:
  truths:
    - "Every locked frame respects the design tokens in app/globals.css (palette, type, grid, breakpoints)"
    - "All copy in frames is English only — no Thai characters"
    - "No production code (app/, components/, lib/, styles/) is modified this phase"
    - "Home (DESIGN-01) locked first; About / Members / Red Wall inherit the Home-set design system"
    - "Each section reviewed in-chat with image attachment; Tew typed 'lock' or 'approved'"
    - "Max 2 iterations per section — third try means the prompt is broken (escalate, rewrite prompt)"
  artifacts:
    - "assets/raw/stitch/01-home/01-home-desktop.png"
    - "assets/raw/stitch/01-home/01-home-tablet.png"
    - "assets/raw/stitch/01-home/01-home-mobile.png"
    - "assets/raw/stitch/02-about/02-about-desktop.png"
    - "assets/raw/stitch/02-about/02-about-tablet.png"
    - "assets/raw/stitch/02-about/02-about-mobile.png"
    - "assets/raw/stitch/03-members/03-members-desktop.png"
    - "assets/raw/stitch/03-members/03-members-tablet.png"
    - "assets/raw/stitch/03-members/03-members-mobile.png"
    - "assets/raw/stitch/tr-red-wall/tr-red-wall-desktop.png"
    - "assets/raw/stitch/tr-red-wall/tr-red-wall-tablet.png"
    - "assets/raw/stitch/tr-red-wall/tr-red-wall-mobile.png"
    - "docs/stitch-prompts.md updated with the final-locked prompt for sections 1–4"
  key_links:
    - "Locked frame palette ↔ app/globals.css @theme tokens (hex must match: #FAF7F2, #1A1A1A, #C1121F, #780000, #D4A373, #4A4A4A, #C9C5BE)"
    - "Locked frame typography ↔ Space Grotesk display, IBM Plex Mono captions, PP Supply Mono eyebrows"
    - "docs/stitch-prompts.md Prompt #N text ↔ the actual prompt run in Stitch that produced the saved PNG"
---

<objective>
Run Google Stitch for Sections 1–4 (Home / About / Members / Red Wall) of pangpuriye6. Generate desktop / tablet / mobile frames per section, iterate with Tew (max 2 cycles each), save locked PNGs to `assets/raw/stitch/<section>/`, and update `docs/stitch-prompts.md` with the final-locked prompt that produced each lock.

Purpose: Lock the visual bar for Phase 03 (Home coding) and Phase 04 (About / Members / Red Wall coding). Home goes first because it sets the design-system baseline that the other three inherit (per D-02).
Output: 12 locked PNG frames (4 sections × 3 viewports), 4 `stitch-url.txt` files for re-edit access, and 4 updated prompt blocks in `docs/stitch-prompts.md`.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-stitch-design-pass/02-SPEC.md
@.planning/phases/02-stitch-design-pass/02-CONTEXT.md
@docs/design.md
@docs/stitch-prompts.md
@app/globals.css
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-01-T01: Lock Home (Section 01) Stitch frames — DESIGN-01</name>
  <files>assets/raw/stitch/01-home/01-home-desktop.png, assets/raw/stitch/01-home/01-home-tablet.png, assets/raw/stitch/01-home/01-home-mobile.png, assets/raw/stitch/01-home/01-home-stitch-url.txt, docs/stitch-prompts.md</files>
  <action>
    Run Stitch for Home (Hero). This is the first section — its lock defines the design-system baseline for sections 2–7 (per D-02).

    Steps:
    1. Open Google Stitch — https://stitch.withgoogle.com. Create one pangpuriye6 project (single project for all 7 sections, per D-01). Save the project URL into `assets/raw/stitch/01-home/01-home-stitch-url.txt` once the project exists.
    2. Confirm `assets/raw/references/obys-home.png` and `assets/raw/references/namma-home.png` exist (Tew should have captured these before this phase started, per D-06). If missing, ask Tew to capture them before continuing.
    3. Paste the shared header + Prompt #1 from `docs/stitch-prompts.md` into Stitch. Attach both reference screenshots in the same prompt (per D-05).
    4. Generate frames at three viewports: desktop 1440×900, tablet 834×1194, mobile 390×844.
    5. Capture the three frames and paste them into the active Claude chat session (per D-09).
    6. Tew responds inline. Greenlight = "lock" or "approved" (per D-10). Otherwise note revision asks.
    7. If revisions are requested, re-prompt Stitch with the corrections. Iteration budget: max 2 iterations (per D-04). If a third revision is needed, STOP — the prompt itself is broken; rewrite Prompt #1 in `docs/stitch-prompts.md` before re-running.
    8. Discard failed iteration outputs (per D-11) — only the locked frame ships.
    9. Save the three locked PNGs to `assets/raw/stitch/01-home/` using the naming convention `01-home-{desktop|tablet|mobile}.png` (per D-08). If Stitch also exports HTML/CSS, save them alongside as `01-home-{viewport}.html` / `.css` (per D-07).
    10. Update `docs/stitch-prompts.md` Prompt #1 section: replace the draft text with the exact final prompt that produced the locked frame (per D-13). Old drafts live in git history — do not keep them in the file.
    11. Capture iteration count + Tew's rejection reasons (if any) for the commit message body (per D-12).

    Design-token verification (reject Stitch outputs that violate any of these — re-prompt or rewrite prompt):
    - Background: cream `#FAF7F2` with subtle film grain.
    - Text: charcoal `#1A1A1A` primary, gray-700 `#4A4A4A` secondary, hairlines `#C9C5BE`.
    - Red surgical (`#C1121F`): used for the single word "PANGPURIYE" in the title and nothing else on Home.
    - Gold (`#D4A373`): ONLY on the four L-shape photo corner crop marks. Nowhere else.
    - Display type: Space Grotesk 700, clamp(56px, 9vw, 144px), tracking -0.03em.
    - Eyebrow: PP Supply Mono uppercase, tracking 0.18em — `SS6 · LEVEL 2 · DIGITAL YEARBOOK · 2026`.
    - Caption under photo: IBM Plex Mono — `FIG.01 · COHORT_PRIME · 2026.05`.
    - 12-col grid, 80px margin, 32px gutter on desktop. Photo aspect ~4:5 in hero placement.
    - All copy English-only. If Stitch generates Thai placeholders, reject and re-prompt.
  </action>
  <what-built>Three locked Stitch frame PNGs for Section 01 (Home) at desktop / tablet / mobile + final prompt text written back into `docs/stitch-prompts.md` Prompt #1.</what-built>
  <how-to-verify>
    1. Open all three PNGs at `assets/raw/stitch/01-home/01-home-{desktop,tablet,mobile}.png`. Confirm visual fidelity to the brief — palette, type, grid, surgical red on PANGPURIYE only, gold only on corner crops.
    2. Confirm `01-home-stitch-url.txt` contains a working Stitch project URL.
    3. Confirm `docs/stitch-prompts.md` Prompt #1 section reflects the EXACT final prompt that produced these frames (not the draft).
    4. Confirm no production code under `app/`, `components/`, `lib/`, `styles/` has been modified — `git diff -- app/ components/ lib/ styles/` should return empty.
    5. Confirm no Thai characters anywhere in the three PNGs or any exported HTML/CSS.
  </how-to-verify>
  <resume-signal>Tew types "lock" or "approved" (or describes the next revision ask, up to 2 iterations total).</resume-signal>
  <verify>
    <automated>test -f assets/raw/stitch/01-home/01-home-desktop.png && test -f assets/raw/stitch/01-home/01-home-tablet.png && test -f assets/raw/stitch/01-home/01-home-mobile.png && test -f assets/raw/stitch/01-home/01-home-stitch-url.txt && grep -q "PANGPURIYE" docs/stitch-prompts.md</automated>
  </verify>
  <done>
    - [ ] Three PNG files exist at `assets/raw/stitch/01-home/01-home-{desktop,tablet,mobile}.png`
    - [ ] `assets/raw/stitch/01-home/01-home-stitch-url.txt` contains Stitch project URL
    - [ ] Each frame uses `#FAF7F2` cream bg, `#1A1A1A` charcoal text, surgical `#C1121F` red (PANGPURIYE word only), gold `#D4A373` only on photo crop marks
    - [ ] Typography: Space Grotesk display + IBM Plex Mono caption + PP Supply Mono eyebrow
    - [ ] Frame copy is English only (no Thai characters)
    - [ ] `docs/stitch-prompts.md` Prompt #1 updated to final-locked version
    - [ ] Tew typed "lock" / "approved" — recorded in commit body as `Reviewed-by: Tew`
    - [ ] Iteration count + any rejection reasons noted in commit body (per D-12)
    - [ ] Failed iteration PNGs discarded (not in `assets/raw/stitch/01-home/`)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-01-T02: Lock About (Section 02) Stitch frames — DESIGN-02</name>
  <files>assets/raw/stitch/02-about/02-about-desktop.png, assets/raw/stitch/02-about/02-about-tablet.png, assets/raw/stitch/02-about/02-about-mobile.png, assets/raw/stitch/02-about/02-about-stitch-url.txt, docs/stitch-prompts.md</files>
  <action>
    Run Stitch for About (Section 02). This section inherits the design system locked in T01.

    Steps:
    1. Open the same Stitch project from T01 (single project for all 7 sections, per D-01). Save its URL into `assets/raw/stitch/02-about/02-about-stitch-url.txt`.
    2. Paste the shared header + Prompt #2 from `docs/stitch-prompts.md` (use the final-locked version after T01's prompt rewrites; the design tokens block is identical).
    3. Attach the same two reference screenshots as T01: `assets/raw/references/obys-home.png` + `assets/raw/references/namma-home.png` (per D-05).
    4. Generate frames at desktop 1440×900, tablet 834×1194, mobile 390×844.
    5. Paste frames into chat for Tew review. Greenlight = "lock"; otherwise revision asks. Max 2 iterations (per D-04).
    6. Discard failed iterations (per D-11).
    7. Save locked PNGs to `assets/raw/stitch/02-about/02-about-{desktop|tablet|mobile}.png` (per D-08). Save Stitch HTML/CSS exports alongside if available (per D-07).
    8. Update `docs/stitch-prompts.md` Prompt #2 with the final-locked text (per D-13).
    9. Record iteration count + rejection reasons for commit body (per D-12).

    Design-token verification (per docs/design.md §3.5 row 02 + Prompt #2):
    - Header band: PP Supply Mono eyebrow `SEC.02 · ABOUT // PANG-A-001`.
    - H2 `About House` in Space Grotesk 700, clamp(40px, 6vw, 88px).
    - Manifesto body left column (7-col desktop). First paragraph leads with giant RED drop cap (`#C1121F`), 5 lines tall, Space Grotesk 700.
    - Motto pull-quote in Space Grotesk italic with vertical GOLD (`#D4A373`) left border — this is one of the two non-corner-crop gold uses on the whole site; confirm it's the 4px-ish vertical border only.
    - Aside right column (5-col desktop): 2×2 grid of 4 framed cards. Each card aspect-ratio 1:1, 1px charcoal hairline border, mono labels — `ASSET: LOGO`, `ASSET: SYMBOL`, `ASSET: UNIFORM`, `DOC: SYS_REV`.
    - Tablet: body stacks above aside; aside grid becomes 4-wide horizontal.
    - Mobile: single column, aside grid 2×2 below body.
    - English copy only.

    NOTE: docs/design.md §2.2 lists the drop cap as a red moment, and the motto's gold left border is a documented exception. Confirm both are present and surgical (no other gold or red on the slide).
  </action>
  <what-built>Three locked Stitch frame PNGs for Section 02 (About) + final prompt text in `docs/stitch-prompts.md` Prompt #2.</what-built>
  <how-to-verify>
    1. Open all three PNGs at `assets/raw/stitch/02-about/02-about-{desktop,tablet,mobile}.png`. Confirm: red drop cap on first paragraph; gold vertical border on motto pull-quote; 4 framed aside cards with charcoal hairlines.
    2. Confirm `02-about-stitch-url.txt` exists and links to the same Stitch project as T01.
    3. Confirm `docs/stitch-prompts.md` Prompt #2 reflects the final-locked prompt.
    4. Confirm no other red or gold accents beyond the documented two.
    5. Confirm no Thai characters anywhere.
  </how-to-verify>
  <resume-signal>Tew types "lock" or "approved" (or describes revision ask, max 2 iterations).</resume-signal>
  <verify>
    <automated>test -f assets/raw/stitch/02-about/02-about-desktop.png && test -f assets/raw/stitch/02-about/02-about-tablet.png && test -f assets/raw/stitch/02-about/02-about-mobile.png && test -f assets/raw/stitch/02-about/02-about-stitch-url.txt</automated>
  </verify>
  <done>
    - [ ] Three PNG files exist at `assets/raw/stitch/02-about/02-about-{desktop,tablet,mobile}.png`
    - [ ] `02-about-stitch-url.txt` contains Stitch project URL
    - [ ] Red `#C1121F` drop cap present on first paragraph
    - [ ] Gold `#D4A373` vertical left border on motto pull-quote (no other gold on slide)
    - [ ] Four aside cards with 1px charcoal hairline borders, mono labels
    - [ ] Typography matches Space Grotesk + IBM Plex Mono + PP Supply Mono triumvirate
    - [ ] English copy only
    - [ ] `docs/stitch-prompts.md` Prompt #2 updated to final-locked version
    - [ ] Tew typed "lock" / "approved" — `Reviewed-by: Tew` in commit body
    - [ ] Iteration count + rejection reasons in commit body (per D-12)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-01-T03: Lock Members (Section 03) Stitch frames — DESIGN-03</name>
  <files>assets/raw/stitch/03-members/03-members-desktop.png, assets/raw/stitch/03-members/03-members-tablet.png, assets/raw/stitch/03-members/03-members-mobile.png, assets/raw/stitch/03-members/03-members-stitch-url.txt, docs/stitch-prompts.md</files>
  <action>
    Run Stitch for Members (Section 03) — the densest visual section, includes a hover-state demonstration.

    Steps:
    1. Open the same Stitch project. Save URL into `assets/raw/stitch/03-members/03-members-stitch-url.txt`.
    2. Paste shared header + Prompt #3 from `docs/stitch-prompts.md`. Attach the two reference screenshots (per D-05).
    3. Generate frames at desktop 1440×900, tablet 834×1194, mobile 390×844. Important: the desktop frame must show ONE member card in the hover state (per Prompt #3 spec) so Phase 04 coders can target the hover treatment.
    4. Paste frames into chat for Tew review. Max 2 iterations (per D-04).
    5. Discard failed iterations (per D-11).
    6. Save locked PNGs to `assets/raw/stitch/03-members/03-members-{desktop|tablet|mobile}.png`.
    7. Update `docs/stitch-prompts.md` Prompt #3 with the final-locked text.
    8. Record iteration count + rejection reasons for commit body.

    Design-token verification (per docs/design.md §3.5 row 03 + Prompt #3):
    - Header: PP Supply Mono eyebrow `SEC.03 · MEMBERS // PANGPURIYE_ROSTER_v1`. H2 `Cohort Roster`. Member count right-aligned in IBM Plex Mono (`6 members`).
    - Toolbar: 5 filter chips uppercase mono — `All / ML / NLP / CV / Ethics / GenAI`. Active chip = solid charcoal bg + cream text. Hover = red `#C1121F` border. Corners 0px (no rounded chips).
    - Grid: 4 cols desktop, 3 tablet, 2 mobile. Card ~360px tall.
    - Card top strip: charcoal hairline border, mono `ID: AIAT-00X` left + red `ACTIVE` right.
    - Photo aspect 4:5, b&w grayscale.
    - Name in Space Grotesk 700 22px; nickname IBM Plex Mono italic 12px; slogan Space Grotesk italic 14px. Interest tags as small bordered chips below.
    - Hover state (one card on desktop frame): photo dims; ID strip slides red `#C1121F` left-to-right; card border becomes red.
    - English copy only. Member names can be `John Doe`-style placeholders.
  </action>
  <what-built>Three locked Stitch frame PNGs for Section 03 (Members) + final prompt text in `docs/stitch-prompts.md` Prompt #3.</what-built>
  <how-to-verify>
    1. Open all three PNGs. Confirm: 5 filter chips (zero rounded corners); 4-col grid desktop / 3-col tablet / 2-col mobile; one card on desktop frame shows hover state (red ID strip, red border, slightly dimmed photo).
    2. Confirm `03-members-stitch-url.txt` exists.
    3. Confirm Prompt #3 updated in `docs/stitch-prompts.md`.
    4. Confirm card photos are b&w / grayscale, not full color.
    5. Confirm English placeholder names only — no Thai characters.
  </how-to-verify>
  <resume-signal>Tew types "lock" or "approved" (or revision ask, max 2 iterations).</resume-signal>
  <verify>
    <automated>test -f assets/raw/stitch/03-members/03-members-desktop.png && test -f assets/raw/stitch/03-members/03-members-tablet.png && test -f assets/raw/stitch/03-members/03-members-mobile.png && test -f assets/raw/stitch/03-members/03-members-stitch-url.txt</automated>
  </verify>
  <done>
    - [ ] Three PNG files exist at `assets/raw/stitch/03-members/03-members-{desktop,tablet,mobile}.png`
    - [ ] `03-members-stitch-url.txt` contains Stitch project URL
    - [ ] 5 filter chips visible with zero rounded corners; one active state shown
    - [ ] Desktop frame shows one member card in hover state (red ID strip slide + red border)
    - [ ] Card photos are b&w / grayscale
    - [ ] Typography stack: Space Grotesk + IBM Plex Mono italic + PP Supply Mono mono accent
    - [ ] English copy only
    - [ ] `docs/stitch-prompts.md` Prompt #3 updated
    - [ ] Tew typed "lock" / "approved" — `Reviewed-by: Tew` in commit
    - [ ] Iteration count + rejection reasons in commit body
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-01-T04: Lock Red Wall (Section TR) Stitch frames — DESIGN-04</name>
  <files>assets/raw/stitch/tr-red-wall/tr-red-wall-desktop.png, assets/raw/stitch/tr-red-wall/tr-red-wall-tablet.png, assets/raw/stitch/tr-red-wall/tr-red-wall-mobile.png, assets/raw/stitch/tr-red-wall/tr-red-wall-stitch-url.txt, docs/stitch-prompts.md</files>
  <action>
    Run Stitch for Red Wall (Section TR) — the SOLE exception to the Red Sniper rule. This entire slide is full-bleed red.

    Steps:
    1. Open the same Stitch project. Save URL into `assets/raw/stitch/tr-red-wall/tr-red-wall-stitch-url.txt`.
    2. Paste shared header + Prompt #4 from `docs/stitch-prompts.md`. Attach the two reference screenshots (per D-05).
    3. Generate frames at desktop 1440×900, tablet 834×1194, mobile 390×844.
    4. Paste frames into chat for Tew review. Max 2 iterations (per D-04).
    5. Discard failed iterations (per D-11).
    6. Save locked PNGs to `assets/raw/stitch/tr-red-wall/tr-red-wall-{desktop|tablet|mobile}.png`. Note the directory slug is `tr-red-wall` (transition slide, between Members and Gallery), not a numbered section.
    7. Update `docs/stitch-prompts.md` Prompt #4 with the final-locked text.
    8. Record iteration count + rejection reasons for commit body.

    Design-token verification (per docs/design.md §3.5 row TR + Prompt #4):
    - Background: SOLID `#C1121F` red, no grain (the grain rule from shared header is intentionally suspended here — entire slide is red).
    - Foreground text: cream `#FAF7F2`.
    - Single sentence centered: `"We forged this house in code."` — Space Grotesk 700, clamp(48px, 7vw, 96px), line-height 1.1.
    - Below sentence (gap 64px): PP Supply Mono uppercase, tracking 0.3em, cream — `— PANGPURIYE, 2026`.
    - NO photo. NO other elements. Top bar (if shown by Stitch) renders in cream over the red — not red on red.
    - Mobile: aim for ~36px font size to keep the sentence on 2 lines max.
    - English copy only.
  </action>
  <what-built>Three locked Stitch frame PNGs for Section TR (Red Wall) — full-bleed red punctuation slide + final prompt text in `docs/stitch-prompts.md` Prompt #4.</what-built>
  <how-to-verify>
    1. Open all three PNGs. Confirm: full-bleed `#C1121F` background; cream `#FAF7F2` text only; centered sentence "We forged this house in code."; PP Supply Mono attribution line below; nothing else on the slide.
    2. Confirm mobile frame keeps sentence to 2 lines max.
    3. Confirm `tr-red-wall-stitch-url.txt` exists.
    4. Confirm Prompt #4 updated in `docs/stitch-prompts.md`.
    5. Confirm no photo, no gold, no charcoal text — pure red + cream only.
    6. Confirm English copy only.
  </how-to-verify>
  <resume-signal>Tew types "lock" or "approved" (or revision ask, max 2 iterations).</resume-signal>
  <verify>
    <automated>test -f assets/raw/stitch/tr-red-wall/tr-red-wall-desktop.png && test -f assets/raw/stitch/tr-red-wall/tr-red-wall-tablet.png && test -f assets/raw/stitch/tr-red-wall/tr-red-wall-mobile.png && test -f assets/raw/stitch/tr-red-wall/tr-red-wall-stitch-url.txt</automated>
  </verify>
  <done>
    - [ ] Three PNG files exist at `assets/raw/stitch/tr-red-wall/tr-red-wall-{desktop,tablet,mobile}.png`
    - [ ] `tr-red-wall-stitch-url.txt` contains Stitch project URL
    - [ ] Background is solid `#C1121F` red (no grain, no gradients, no photos)
    - [ ] Foreground text is cream `#FAF7F2` only
    - [ ] Sentence "We forged this house in code." rendered in Space Grotesk 700
    - [ ] Attribution line in PP Supply Mono uppercase tracking 0.3em
    - [ ] Mobile frame keeps sentence to 2 lines max
    - [ ] English copy only
    - [ ] `docs/stitch-prompts.md` Prompt #4 updated
    - [ ] Tew typed "lock" / "approved" — `Reviewed-by: Tew` in commit
    - [ ] Iteration count + rejection reasons in commit body
  </done>
</task>

</tasks>

<verification>
  After all four tasks complete:
  1. `ls assets/raw/stitch/01-home/ assets/raw/stitch/02-about/ assets/raw/stitch/03-members/ assets/raw/stitch/tr-red-wall/` shows desktop / tablet / mobile PNGs + stitch-url.txt in each.
  2. `git diff -- app/ components/ lib/ styles/` returns empty (no production code modified this plan).
  3. `docs/stitch-prompts.md` Prompts #1–#4 reflect the exact final-locked text per section (not the drafts).
  4. Commit message body includes iteration counts + rejection reasons per section + `Reviewed-by: Tew` tag.
  5. No Thai characters anywhere in PNGs or exported HTML/CSS.
  6. `assets/raw/stitch/` is gitignored — confirm with `git check-ignore assets/raw/stitch/01-home/01-home-desktop.png` (exit code 0 means correctly ignored). Note: this plan tracks artifact creation; whether they get committed depends on `.gitignore` rules + Tew's choice. Per SPEC.md, only `docs/stitch-prompts.md` text changes are committed.
</verification>

<success_criteria>
  - DESIGN-01 (Home) locked: 3 frames + final prompt + Tew approval.
  - DESIGN-02 (About) locked: 3 frames + final prompt + Tew approval.
  - DESIGN-03 (Members) locked: 3 frames + final prompt + Tew approval.
  - DESIGN-04 (Red Wall) locked: 3 frames + final prompt + Tew approval.
  - All locked frames pass design-token fidelity check (palette, type, grid).
  - All copy is English only.
  - No production code modified this plan.
  - `docs/stitch-prompts.md` is the current source of truth — Prompts #1–#4 match the prompts that produced the saved PNGs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stitch-design-pass/02-01-SUMMARY.md` per the execute-plan template, capturing:
- Frames locked per section (with iteration count)
- Tew's rejection notes per iteration
- Any prompt rewrites required (track drift from original draft)
- Decision points where the Red Sniper rule was tested
- Forward signal to Phase 03 coders: what photo treatments / type weights / spacing pixel-targets the locked frames imply
</output>
